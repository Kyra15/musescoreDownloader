services:
  - type: web
    name: musescore-downloader
    env: python
    buildCommand: "pip install -r requirements.txt && patchright install chromium && patchright install-deps"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0